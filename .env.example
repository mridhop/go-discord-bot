APP_ID=
APP_PUBLIC_KEY=
DISCORD_BOT_TOKEN=
# Optional: set to a guild ID for instant command updates during development.
# Leave empty to register commands globally (propagation can take up to an hour).
DISCORD_GUILD_ID=
# Path to the SQLite database file. Defaults to bot.db if not set.
DATABASE_PATH=bot.db
